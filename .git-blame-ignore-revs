# Bulk reformat commits — ignored by git blame.
# Configure locally with:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Add the hash of this reformat commit once it is made.
76ee91b  # Simplify constexpr guards, enrich hooks, reformat all C++ files
